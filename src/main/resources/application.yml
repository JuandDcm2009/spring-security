server:
    port: 8090

spring:
    datasource:
        url: jdbc:mysql://localhost:3309/spring_security?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC&connectionCollation=utf8mb4_unicode_ci
        username: root
        password: ${DB_PASSWORD:admin}
        driver-class-name: com.mysql.jdbc.Driver

    jpa:
    #    defer-datasource-initialization: true
        hibernate: 
            ddl-auto: create
        show-sql: true
        properties: 
            hibernate:
                format-sql: true
    #sql:
    #    init:
    #        mode: always